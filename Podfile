platform :ios, '13.0'

use_frameworks! :linkage => :static

target 'On-Air SwiftUI' do

  # Pods for On-Air SwiftUI
  pod 'Firebase/Firestore'
  
  pod 'Firebase/Storage'
  
  pod 'SDWebImageSwiftUI', :modular_headers => true
  

end
